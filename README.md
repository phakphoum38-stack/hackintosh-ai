# Hackintosh AI Platform

AI system that:
- predicts boot success
- generates EFI automatically
- runs VM boot testing
- learns from failures

## Run

```bash
pip install -r requirements.txt
docker-compose up


---

# 🐳 10️⃣ ทำให้ GitHub run ได้เลย (CI/CD optional)

เพิ่ม:

```text id="gh10"
.github/workflows/build.yml
